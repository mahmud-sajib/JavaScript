jQuery(document).ready(function($) {

    // get height of the content box
	var getHeight = $("div.container").height();
	console.log(getHeight);
    
    // set width of the content box
	var setWidth = $("div.container").width(10);
	
});